
  function getInputValue(){
      // Selecting the input element and get its value 
      var inputVal = document.getElementById("customAmount").value + "00";
      console.log(inputVal);
      // var formatInput = inputVal + "00";
  }


